/*int findClosest(int x, int y, int z) {

    return abs(x-z) == abs(y-z) ? 0 : abs(x-z) < abs(y-z) ? 1 : 2;
    
}*/


int findClosest(int x, int y, int z) {

    int d1=abs(x-z);    
    int d2=abs(y-z);

    if(d1 < d2)
    {
        return 1;
    }
    else if(d1 > d2)
    {
        return 2;
    }
    else
    {
        return 0;
    }    
}
