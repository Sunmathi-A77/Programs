bool isVowel(char c) 
{
    return c=='a' || c=='e' || c=='i' || c=='o' || c=='u';
}

bool doesAliceWin(char* s) 
{
    for (int i = 0; s[i] != '\0'; i++) 
    {
        if (isVowel(s[i])) 
        {
            return true;
        }
    }
    return false;
}
