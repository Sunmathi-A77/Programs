// User function Template for Java

class Solution {
    int totalCount(int k, int[] arr) {
        
        int total=0;
        
        for(int i=0;i<arr.length;i++)
        {
            total+=(arr[i]+k-1)/k;
        }
        
        return total;
    }
}
