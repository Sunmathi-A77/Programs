class Solution:
    def altProduct(self, arr):
        
        arr.sort()
        
        left = 0
        right = len(arr)-1
        
        sum = 0
        
        while(left < right):
            sum += arr[left] * arr[right]
            left += 1
            right -= 1
            
        return sum
