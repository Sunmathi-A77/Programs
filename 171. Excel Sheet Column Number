/*int titleToNumber(char* columnTitle) {

    long long result=0;
    long long power=1;

    for(int i=strlen(columnTitle)-1;i>=0;i--)
    {
        result=result+(columnTitle[i]-'A'+1)*power;
        power*=26;
    }
    return (int)result;
}*/


int titleToNumber(char* columnTitle) {
    long long result = 0;

    for (int i = 0; columnTitle[i] != '\0'; i++) {
        result = result * 26 + (columnTitle[i] - 'A' + 1);
    }

    return (int)result;
}
