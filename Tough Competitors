class Solution:
    def minDiff(self, arr):
        
        arr.sort()
        min_diff = float('inf')

        for i in range(1, len(arr)):
            diff = arr[i] - arr[i-1]
            min_diff = min(min_diff, diff)

        return min_diff
