/*int countOdds(int low, int high){

    int oddcount=0;

    for(int i=low;i<=high;i++)
    {
        if(i%2==1)
        {
            oddcount++;
        }
    }

    return oddcount;
}*/

int countOdds(int low, int high) {

    int total = (high - low) / 2;

    if (low % 2 != 0 || high % 2 != 0) 
    {
        total++;
    }

    return total;
}
